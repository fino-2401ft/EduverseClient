<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.eduverseclient.controller.ExamRoomController"
            prefHeight="800.0" prefWidth="1400.0"
            style="-fx-background-color: #1E1E1E;">

    <!-- TOP: Exam Info + Timer -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" style="-fx-background-color: #2C2C2C; -fx-padding: 15;">
            <Label fx:id="examTitleLabel" text="Bài thi"
                   style="-fx-text-fill: white; -fx-font-size: 18; -fx-font-weight: bold;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="timerLabel" text="⏱ 00:00:00"
                   style="-fx-text-fill: #4CAF50; -fx-font-size: 16; -fx-font-weight: bold;"/>
            <Label fx:id="cameraStatusLabel" text="📹 Camera: BẮT BUỘC BẬT"
                   style="-fx-text-fill: #4CAF50; -fx-font-size: 13;"/>
        </HBox>
    </top>

    <!-- CENTER: Main Content -->
    <center>
        <HBox spacing="10" style="-fx-padding: 10;">

            <!-- LEFT: Video Grid (giống meeting) -->
            <VBox spacing="10" prefWidth="600" style="-fx-background-color: #1E1E1E;">
                <Label text="📹 Video" style="-fx-text-fill: white; -fx-font-size: 16; -fx-font-weight: bold;"/>
                <GridPane fx:id="videoGrid" hgap="10" vgap="10"
                          style="-fx-background-color: #1E1E1E;"
                          VBox.vgrow="ALWAYS">
                    <columnConstraints>
                        <ColumnConstraints percentWidth="50"/>
                        <ColumnConstraints percentWidth="50"/>
                    </columnConstraints>
                    <rowConstraints>
                        <RowConstraints percentHeight="50"/>
                        <RowConstraints percentHeight="50"/>
                    </rowConstraints>
                </GridPane>
            </VBox>

            <!-- RIGHT: Exam Questions Panel (Student) / Violations Panel (Proctor) -->
            <VBox spacing="10" HBox.hgrow="ALWAYS" style="-fx-background-color: #2C2C2C; -fx-padding: 15; -fx-background-radius: 10;">
                <!-- Proctor: Violations Panel Header -->
                <HBox fx:id="violationsHeader" alignment="CENTER_LEFT" spacing="10" managed="false" visible="false">
                    <Label text="🚨 Cảnh báo gian lận" style="-fx-text-fill: white; -fx-font-size: 16; -fx-font-weight: bold;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                </HBox>
                
                <!-- Student: Questions Panel Header -->
                <HBox fx:id="questionsHeader" alignment="CENTER_LEFT" spacing="10">
                    <Label text="📝 Bài kiểm tra" style="-fx-text-fill: white; -fx-font-size: 16; -fx-font-weight: bold;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button fx:id="addQuestionButton" text="➕ Thêm câu hỏi" onAction="#handleAddQuestion"
                            style="-fx-background-color: #1976D2; -fx-text-fill: white;
                                   -fx-pref-width: 130; -fx-pref-height: 35; -fx-background-radius: 5;
                                   -fx-font-size: 13;"/>
                </HBox>
                
                <!-- Alert Container (chỉ hiển thị cho student) -->
                <VBox fx:id="alertContainer" spacing="5" style="-fx-padding: 5;" managed="false" visible="false"/>
                
                <!-- Violations Panel (chỉ cho proctor) -->
                <ScrollPane fx:id="violationsScrollPane" fitToWidth="true" VBox.vgrow="ALWAYS"
                            style="-fx-background-color: #1E1E1E; -fx-background-radius: 5;" managed="false" visible="false">
                    <VBox fx:id="violationsPanel" spacing="10" style="-fx-padding: 10;"/>
                </ScrollPane>
                
                <!-- Questions Panel (chỉ cho student) -->
                <ScrollPane fx:id="questionsScrollPane" fitToWidth="true" VBox.vgrow="ALWAYS"
                            style="-fx-background-color: #1E1E1E; -fx-background-radius: 5;">
                    <VBox fx:id="questionsContainer" spacing="15" style="-fx-padding: 10;"/>
                </ScrollPane>

                <!-- Bottom: Submit Button -->
                <HBox alignment="CENTER_RIGHT" spacing="10" style="-fx-padding: 10;">
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button fx:id="submitButton" text="📤 Nộp bài" onAction="#handleSubmitExam"
                            style="-fx-background-color: #4CAF50; -fx-text-fill: white;
                                   -fx-pref-width: 150; -fx-pref-height: 45; -fx-background-radius: 5;
                                   -fx-font-size: 14; -fx-font-weight: bold;"/>
                    <Button fx:id="leaveButton" text="📞 Rời phòng thi" onAction="#handleLeave"
                            style="-fx-background-color: #E53935; -fx-text-fill: white;
                                   -fx-pref-width: 150; -fx-pref-height: 45; -fx-background-radius: 5;
                                   -fx-font-size: 14;"/>
                </HBox>
            </VBox>
        </HBox>
    </center>

</BorderPane>

